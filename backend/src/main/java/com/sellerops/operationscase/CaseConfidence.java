package com.sellerops.operationscase;

/** The investigator's own confidence, as a closed token. LOW is never allowed to close anything. */
public enum CaseConfidence {
    LOW,
    MEDIUM,
    HIGH
}
