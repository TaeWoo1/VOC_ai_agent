{% extends "base.html.j2" %}
{% block page %}
<div class="page page--consider">
  <div class="consider-chip">{{ page.chip }}</div>
  <h1 class="consider-title">{{ page.title }}</h1>
  {% if page.subtitle and page.subtitle != page.title %}<div class="consider-subtitle">{{ page.subtitle }}</div>{% endif %}

  {% if page['items'] %}
  <div class="consider-list">
    {% for it in page['items'] %}
    <div class="consider-card">
      <div class="consider-card-label">{{ it.label }}</div>
      {% if it.signal_hint %}<div class="consider-card-hint">{{ it.signal_hint }}</div>{% endif %}
      <div class="consider-card-note">{{ it.note }}</div>
    </div>
    {% endfor %}
  </div>
  {% endif %}

  {% if page.aux_block %}
  <div class="aux-block">
    <div class="aux-block-title">{{ page.aux_block.title }}</div>
    <div class="aux-block-text">{{ page.aux_block.text }}</div>
  </div>
  {% endif %}

  <div class="page-index">— {{ page.index }} / {{ total_pages }} —</div>
</div>
{% endblock %}
