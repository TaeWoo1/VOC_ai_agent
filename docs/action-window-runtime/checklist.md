# Checklist — Action Window Runtime

Durable progress ledger. Update **only at meaningful implementation /
verification milestones**, not per tiny edit. Every `VERIFIED` or `MERGED`
requires linked evidence.

Status vocabulary (only these):
`NOT_STARTED` · `IN_PROGRESS` · `BLOCKED` · `IMPLEMENTED` · `VERIFIED` · `MERGED`
· `DEFERRED`

> **Rule:** do not mark an existing primitive as *Action-Window-complete* merely
> because related code exists. Reuse ≠ delivered Action Window capability.

| # | Item | Status | Code evidence | Test evidence | Fixture/live evidence | Commit/PR | Notes / blocker |
|---|---|---|---|---|---|---|---|
| 1 | Contract readiness (R0) | `MERGED` | `contracts/action-window/v1/index.ts`, `schema.json` | `collector/test/contracts/action-window/contract.test.ts` — 55/55 | 25 valid + 10 negative fixtures | PR #212, merge `026eb77` | Protocol v1; nested-in-Bridge-v1 transport; schema↔TS consistency green. |
| 2 | State engine (R1) | `VERIFIED` | `collector/src/action-window/engine.ts`, `stages.ts` | `test/action-window/engine.test.ts` — 20 (incl. canonical-contract regression) | headed QA `normal` → 13-event loop → COMPLETED (canonical contract) | `feat/action-window-runtime-r1` → reconciled on `fix/action-window-runtime-contract` | Channel-neutral pure engine; contract-valid events/view; aligned to post-#214 canonical shape. |
| 3 | Target detection | `VERIFIED` | `collector/src/action-window/locator.ts`, `signature.ts` | `fixture-browser.test.ts` (RUN_INTEGRATION) | 0/1/many synthetic + headed QA | `feat/action-window-runtime-r1` | In-page 16-hex signature; no selector/text leaves page; fails closed on 0/many. |
| 4 | Overlay | `VERIFIED` | `collector/src/action-window/overlay.ts` | `fixture-browser.test.ts` — mount + no-intercept + reposition + guidance-off | synthetic + headed QA | `feat/action-window-runtime-r1` | `pointer-events:none` (never intercepts click); repositions on layout move. |
| 5 | Actual user-click observation | `VERIFIED` | `collector/src/action-window/observer.ts` | `fixture-browser.test.ts` | headed QA: real human click → `USER_ACTION_OBSERVED` | `feat/action-window-runtime-r1` | Records sanitized boolean only; Runtime never clicks; click ≠ completion. |
| 6 | Transition verification | `VERIFIED` | `collector/src/action-window/verifier.ts` | `engine.test.ts` + `fixture-browser.test.ts` | headed QA: verified → COMPLETED | `feat/action-window-runtime-r1` | Verified transition is the sole completion authority; unchanged → no false completion. |
| 7 | Fail-closed cases | `VERIFIED` | `engine.ts` (`fail()`), `verifier.ts`, `locator.ts` | `engine.test.ts` + `fixture-browser.test.ts` | headed QA `multi-candidate` → TARGET_AMBIGUOUS, no click | `feat/action-window-runtime-r1` | TARGET_NOT_FOUND / AMBIGUOUS / UI_DRIFT / UNSUPPORTED_STATE; stale-rev + proto-version rejected; cancel cleans up. |
| 8 | Dummy downstream | `VERIFIED` | `collector/src/action-window/engine.ts` (`runDownstream`) | `engine.test.ts` | headed QA: `downstream.processed=1` | `feat/action-window-runtime-r1` | One deterministic in-memory step; no backend/upload/download. |
| 9 | Contract events (in-memory sink) | `VERIFIED` | `collector/src/action-window/events.ts`, `engine.ts` | `engine.test.ts` — validate + sequence/revision | headed QA: 13-event ordered loop | `feat/action-window-runtime-r1` | Sanitized + ordered to in-memory sink; **real Bridge transport handlers = R2**. |
| 10 | Cleanup | `VERIFIED` | `harness.ts` (`finish`), `overlay.ts`, `observer.ts` | `fixture-browser.test.ts` — overlay removed + flag cleared | headed QA: clean process exit | `feat/action-window-runtime-r1` | Overlay/listeners torn down on complete/cancel/fail; browser owned by harness only. |
| 11 | Privacy / sanitization | `VERIFIED` | `view.ts`, `signature.ts`, engine payloads | `engine.test.ts` + `fixture-browser.test.ts` — `findProhibitedFields` == [] | headed QA: opaque 16-hex `targetRef` only | `feat/action-window-runtime-r1` | Only enums/counts/opaque-16-hex; no selector/text/URL/path in any event or view. |
| 12 | FE integration (R2A — offline FE/Runtime integration) | `VERIFIED` | transport `contracts/action-window/v1/transport.ts`; Runtime `collector/src/action-window/session.ts`, `browser-driver.ts`; FE `frontend/src/lib/actionWindow/bridgeAdapter.ts`, `controller.ts`, `devMode.ts` | `test/action-window/session-integration.test.ts` — 8 (offline loopback E2E); FE `bridgeAdapter.test.ts` — 9 | `test/action-window/session-browser.test.ts` — **RUN**: automated headless (simulated click) + **headed operator proof (real human click)**, both green (`RUN_INTEGRATION=1 [AW_HEADED=1]`) | `integ/action-window-v1`, PR #217 | FE↔loopback↔Runtime E2E green offline AND against real Chromium: start→checkpoint(overlay visible)→**real user click (observation ≠ completion)**→recheck→verify→downstream→completed, clean teardown (no overlay/observer/timer left). Plus stale/dup/pause/resume/cancel/reconnect/privacy offline. Transport rides **inside** Bridge v1 as opaque frames — `bridge/protocol.ts` unchanged. **R2B (real Bridge-WS passthrough transport) deferred** — `resolveBridgeSession()`→null, shipped FE stays on mock. No live channel. |

> **R1.1 (contract reconciliation).** PR #214 rewrote the contract shape (modes,
> `channelCode`, `copyKey`/`copyParams`, prose→prohibited) **without bumping
> `ACTION_WINDOW_PROTOCOL_VERSION`**; R1 (#213) merged on top unreconciled and broke
> `main`'s collector typecheck. `fix/action-window-runtime-contract` realigns R1 to
> the canonical contract (verified: 20 unit + 8 browser + headed QA). **Governance
> rule:** future breaking contract changes require a version bump or explicit
> migration, and Runtime must be typechecked against the contract on `main`.
| 12b | Bridge-WS passthrough (R2B — live transport) | `VERIFIED` (offline, real loopback WS) | Runtime `collector/src/bridge/action-window-endpoint.ts` (+ opaque `{type:"aw"}`/`aw_session` relay in `bridge-server.ts`, hosting in `agent-bridge.ts`, DEV-only `--dev-action-window-synthetic` in `cli/local-agent.ts`); FE `frontend/src/lib/actionWindow/wsTransport.ts`, real async `resolveBridgeSession()` (`devMode.ts`) + runtime mock fallback (`controller.ts`) | collector `test/action-window/bridge-transport.test.ts` — 10 (real pairing→ticket→WS; full loop, reconnect replay, stale/dup, cancel, unauthorized/unpaired, malformed drop, broadcast-vs-reply routing, prod gate); FE `wsTransport.test.ts` — 8, `devMode.test.ts` — 3 | offline synthetic driver over a REAL loopback WebSocket; wire privacy `findProhibitedFields` == [] on every frame | PR #218, merge `115582a` | Frames nest inside Bridge v1 as opaque carriers — `bridge/protocol.ts` unions untouched, contract dir untouched, no version bump. Pairing/ticket/origin security reused (no separate AW socket/ticket). FE reuses `BRIDGE_TOKEN_KEY`; run identity is agent-announced (`aw_session`); reconnect = fresh ticket + `aw_resync` from 0 (idempotent replay); run-identity pinned across reconnects. No live channel; production hosts no session. |
| 13 | Persistence (R3) | `VERIFIED` (offline) | engine `runState()`/`restore()`/`pauseForRestore()` (`collector/src/action-window/engine.ts`); pure domain `operation-run.ts` (OperationRun/OperationTask/HumanCheckpoint/ResumeState, restore planning, strict parse); `run-store.ts` (atomic writes, sanitized errors, prohibited-content gate on save+load); `run-lifecycle.ts` (create/resume/open-or-resume); session `onStatePublished` hook; `agent-bridge`/`local-agent` `persistDir` wiring (`.operation-runs/`, gitignored) | `operation-run-persistence.test.ts` — 9 (create/load, restart→checkpoint restore, cross-restart duplicate no-op + stale rejection, idempotent downstream resume, failed-run resume + re-fail-closed, terminal protection, audit ordering across restart, privacy); `run-store.test.ts` — 10 | offline synthetic driver over loopback; restart simulated by full rebuild from disk; `findProhibitedFields` == [] on every persisted record; backend re-verified untouched (`gradlew test` 984/0 on H2) | branch `feat/operation-run-persistence` (this slice's PR) | Re-authors `work/*` patterns (non-positional commandId ledger, verified-transition-only completion, append-only ordered audit) for the AW engine; agent-LOCAL store — the plan's "caller-less `CollectionRunService`" premise found stale (already upload-wired, flat `sync_jobs`, no step/checkpoint tables) and backend mirroring is ruled a PO decision (D-018), not silently implemented. Resumable runs park at PAUSED (explicit `RESUME_RUN` only); COMPLETED/CANCELLED never restart. |
| 14g | Channel-neutral downstream engine (R4 groundwork) | `IMPLEMENTED` (offline-verified, **uncommitted** — upgrade to `VERIFIED` at commit/PR) | engine downstream chain `DETECT_DOWNLOAD → VALIDATE_ARTIFACT → INGEST_HANDOFF` (`collector/src/action-window/engine.ts`, `stages.ts`); `ProbeDriver.detectDownload/validateArtifact/ingest` + synthetic/fixture drivers (`session.ts`, `browser-driver.ts`, `harness.ts`); opaque artifact ref helper (`artifact.ts`); restore policy re-enters downstream at `DETECT_DOWNLOAD`, `OPERATION_RUN_SCHEMA_VERSION = 2` (`operation-run.ts`) | `engine.test.ts` (downstream happy path + `DOWNLOAD_TIMEOUT`/`ARTIFACT_INVALID`/ingest-failure fail-closed, malformed ref never emitted); `operation-run-persistence.test.ts` (resume between verify↔detect and validate↔ingest, `DOWNLOAD_TIMEOUT` resumes through the checkpoint, privacy); `run-store.test.ts` (legacy v1 record fails closed); `artifact.test.ts`; collector offline suite 2455/12 skip, typecheck green | offline synthetic only — no live channel, no real download, no backend | branch `feat/r4-supervised-channel-runtime` (this slice's PR) | Uses ONLY reserved contract codes (`DOWNLOAD_DETECTED`, `DOWNLOAD_TIMEOUT`, `ARTIFACT_INVALID`) — contract dir untouched. Stale dev v1 `.operation-runs/` records fail closed (`WRONG_SCHEMA_VERSION`); delete locally. FE coordination: step-3 copy key is now `actionWindow.step.downstream`. No ingest-specific blocker code reserved — failed handoff uses generic `UNSUPPORTED_STATE`; a dedicated code is a governed contract change deferred to the adapter slice. |
| 14 | First pilot adapter (R4) | `NOT_STARTED` (groundwork done → see 14g; preparation docs → see 14p) | seams: `export-target-readiness*.ts`, `collector/src/upload.ts`, downstream driver interface (14g) | — | — | — | Channel **not final and not automatically ESM+** (D-020: five weighted criteria; current evidence favors NAVER review export on criteria 2–5, pilot-company channel mix decisive and unresolved). Adapter code blocked on the `r4-preparation.md` §3 gate (G1 first); live additionally requires per-run PO approval; policy/provider track runs in parallel (D-019). |
| 14p | R4 preparation (readiness docs) | `IMPLEMENTED` (docs) | `r4-preparation.md` — readiness checklist, selection criteria + evidence scoring, internal gate, safety boundary, policy/provider checklist, adapter readiness checklist, rollback/abort, pre-live evidence pack; D-019/D-020 appended | markdown link/path check, `git diff --check`, secret/private-path scan (docs-only slice) | n/a (no code, no live contact) | branch `prep/r4-supervised-channel` (this slice's PR) | Blocking PO decisions enumerated in `r4-preparation.md` §9 — channel selection cannot proceed from repo evidence alone. |

## Reused-foundation register (NOT Action Window capability)

These exist and are reusable, but are **not** Action Window deliverables until a
row above is `VERIFIED`:

- connection profile resolver — `connectionProfileDirFor`,
  `resolveCaptureConnectionProfile`
- candidate signature / frame scan — `esm-candidate-signature.ts`,
  `esm-frame-scan.ts`
- fail-closed gate / sentinel — `esm-capture-gate.ts`, `esm-sentinel.ts`
- download readiness (read-only) — `export-target-readiness.ts`,
  `export-target-readiness-stable.ts`
- upload / ingestion handoff — `collector/src/upload.ts`
- work/run/audit domain — `collector/src/work/*`
- Bridge protocol — `collector/src/bridge/protocol.ts`
- Browser Projection (optional renderer, State B) — `collector/src/bridge/projection-*`
