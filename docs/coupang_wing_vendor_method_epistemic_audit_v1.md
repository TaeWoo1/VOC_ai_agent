# Vendor-Method Epistemic Audit — 2026-08-12, before the WRITE run

Every vendor-method claim in code, manifest and evidence, filed under what actually supports it. No new
measurement was taken. Three corrections landed; they are marked ⓕ below and are all narrowings.

Audited at `b813a7e1`; corrections at the SHA this document ships with.

---

## 1. Run provenance — which sittings may be cited

| record | approval / sha | how each checkpoint was signalled | citable |
|---|---|---|---|
| `wingrec_0653cb92f342` | `apr-87fb0614a39f` @ `0dfb6929` | cp1–cp3 signalled by Claude from chat turns. **cp3's `3번 됐어` was generated by Claude, not sent by the operator** | **NO — excluded** |
| `wingrec_d5dc00c2486c` | `apr-b65060f5857e` @ `0dfb6929` | cp1–cp2 by Claude from genuine turns (one a screen description); **cp3–cp5 by the operator's own `touch`** | corroboration only |
| `wingrec_c7d61cd70f63` | `apr-5f58409446fd` @ `7049af08` | **all six by the operator's own `touch`. Claude never touched the sentinel** | **YES — the evidence base** |

Verified: the excluded run's `wingrec_`/`apr-`/`wt-` ids appear in **no** source, manifest or evidence file, and
a test greps for them. Every vendor-screen promotion cites `wingrec_c7d61cd70f63` only.

ⓕ The marker refutation was anchored to `wingrec_d5dc00c2486c`; it is re-anchored to the direct-sentinel run,
which refutes it independently on all six readings. The earlier sitting now reads as agreement, not as source.

---

## 2. LIVE_MEASURED

All from `wingrec_c7d61cd70f63`, two checkpoints of the vendor screen, agreeing integer for integer and tag for
tag. Broad siblings on the record beside each narrowing.

| claim | reading |
|---|---|
| the vendor-method screen exists and is reached by pressing `약관 동의 및 Key 발급받기` | screens `PURPOSE → PURPOSE → TERMS → TERMS → VENDOR_METHOD → VENDOR_METHOD`, no halt |
| `연동업체 선택` as `label` | visible 1, hidden 1, `LABEL`, ×2 · broad = 2 |
| `자체개발(직접입력)` as `label` | visible 1, hidden 1, `LABEL`, ×2 · broad = 2 |
| the screen's `확인` (`button,a`) | visible 1, hidden 12, `BUTTON`, ×2 |
| `업체명` | visible 1, hidden 3, `DT`, ×2 — **already painting before any selection** |
| two visible radios, one group, both named by `label[for]`, names = the two option strings | association census, ×2, indices 8 and 9 |
| the vendor screen **replaces** the terms screen (both terms markers go hidden on it) | ×2 |
| `OPEN API 키 발급` paints on **every** screen in the flow (1,1,1,1,2,2) | ×6 — why it is not a marker |
| `업체 입력 방식` is never any element's whole text, **including on its own screen** | ×6 — the shipped marker, refuted |
| `URL` / `IP 주소` painted for the first time at the checkpoint after a method was selected | **×1 only — not promotable** |
| SellerOps cannot tell an issued surface from a no-key one | `NO_DISCRIMINATING_SIGNAL`, measured on both surfaces |

## 3. OPERATOR_REPORTED_NOT_YET_LIVE_VERIFIED

| claim | status |
|---|---|
| **the vendor screen's `확인` issues the key** | operator's account. No apparatus has pressed it. **The WRITE run is what verifies it.** `WING_KEY_ISSUING_CONTROL` carries `OPERATOR_REPORTED_NOT_MEASURED` |
| **pressing `약관 동의 및 Key 발급받기` issues no key** | operator's report on two live walks. ⓕ Was written as an apparatus measurement in eight operator-facing places — corrected. SellerOps is structurally unable to confirm it; the only thing the runtime adds is negative and weak (the page category never became `credential_shown`) |
| the three fields are things the seller types into | the labels are measured; the inputs behind them are not enumerated |
| a key created here is irreversible / affects an existing key | never measured, and the manifest errs toward more caution rather than less |

The two `operatorIssuesRealKey: true` / "ISSUES THE KEY, IRREVERSIBLY" statements in the manifest rest on this
row. They are deliberately kept at their strongest: a warning that overstates the risk of an irreversible act
errs in the safe direction, and the code-level record says plainly which row it comes from.

## 4. PRODUCT_OWNER_DECISION

| decision | record |
|---|---|
| SellerOps V1 uses **`자체개발(직접입력)`** | `WING_VENDOR_METHOD_PRODUCT_DECISION`, `basis: PRODUCT_DECISION_NOT_A_MEASUREMENT`, `decidedBy: PRODUCT_OWNER` (2026-08-12). Mirrored in the walk descriptor as `vendorMethodGuided` + `vendorMethodDecidedBy`, both machine-checked by the shell fence |
| `연동업체 선택` is not ringed | measured to the same standard; absent from the ringable vocabulary rather than sitting in the promotion table as an unpromoted row, which would read as something still owed |
| what would have to be established for the alternative | `WHETHER_SELLEROPS_IS_IN_COUPANGS_VENDOR_LIST` — external, not measurable here |

**Which method the operator actually selected on the measured run is not recorded**, and the URL/IP reveal is
not attributed to either option. ⓕ One doc and the seller-facing copy read the reveal as three fields; `업체명`
was already on screen, so it is two.

---

## 5. Corrections made (all narrowings)

1. ⓕ `keyIssuedAttribution` added to the refutation record; the eight "MEASURED not to create a key" sites now
   attribute the report. A tree-wide grep guard keeps the phrasing out.
2. ⓕ `COUPANG_ISSUANCE_KEY_CREATION_STEP` claimed "the boundary is measured". The screen is measured; the
   consequence is not.
3. ⓕ The reveal narrowed from three fields to two, in the record and in the seller's copy.

No promotion, ring, auto-advance or step changed. 8147 collector tests + 1883 frontend tests green; probe, walk
and reveal selfchecks PASS.

## 6. Action Window principles — unchanged

Operator performs every marketplace action; agent click/input/submit/select budget is 0; every advance is change
detection on WING's own state, and the key-issuing step advances only on the credentials **already** being shown
— an observation of the result, which cannot cause it, and which is disabled when the baseline already reads
that way.
