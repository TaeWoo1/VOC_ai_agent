# SellerOps Frontend Spec — 통합 셀러센터 리디자인 정본

> Status: **CANONICAL (frontend).** SellerOps 프론트엔드 리디자인의 단일 source of truth.
> 작성 2026-07-07. 근거: 프론트 정찰(코드 전수 열람)·문서 감사(2026-07-07 세션) + 제품 결정.
>
> **2026-07-26 갱신 (Agent-first / UI-light — 제품 오너 결정 반영).** 정본:
> `docs/action-window-runtime/agent-first-ui-light-adr.md`(Accepted), 범위: `docs/product-scope-v1.md`
> §1.7·§1.8·§5.2(lock v1.7). ① **FE = Agent Control Plane projection/command adapter** — FE는 Journey
> 상태나 실행 순서를 **소유하지 않는다**; 런타임이 투영하는 것을 렌더하고 런타임이 인가하는 command만
> 보낸다(포트: `JourneyProjectionPort`/`JourneyCommandPort`, 기존 Bridge/WS 전송은 그 한 어댑터). 실행은
> 컴포넌트 mount에 의존하지 않는다. ② **기존 FE 신규 기능 동결** — 능력마다 전용 page/card/hook을 더하지
> 않는다; 신규 능력은 기본적으로 기존 포트로 투영되는 **state/command**이지 새 화면이 아니다. 기존 FE는
> 삭제하지 않고 **임시 호환 어댑터**로만 유지한다. ③ **기본 UX = pull-first / exception-push**
> (product-scope §1.8) — "매일 여는 대시보드" 프레이밍은 대체된다(§3 규칙 1·§9 supersede). 미래 FE는 **확인할
> 일·진행 중·완료 결과·연결 상태**만 있는 minimal Control Center로 수렴하고, 방향은 공통 **OperationView +
> HumanCheckpoint**다. ④ **답변 초안 = Company Voice 기반 AI**(product-scope §5.2) — §10.3의 "규칙 기반·AI
> 없음"은 대체된다; 단 **발송 없음/승인 후 Capability 발송·"발송/등록" 라벨 금지·`UNVERIFIED` 쌍 표기**의
> FE 정직성 경계(§10.2·§15·§18)는 **그대로 유지**된다(FE가 라벨을 소유). 이 갱신은 어떤 채널 capability도
> 승격하지 않는다(§15).
>
> 2026-07-08 갱신(제품 오너 결정 반영): ① **frontstage 초점 재정의** — 에이전트 활동·완료된 자동 작업·
> 사람 체크포인트·긴급 운영 신호·실패/정체 연결·제안 다음 행동·운영 결과 중심(§3·§9). 셀러가 제품을
> 커넥터-관리 콘솔로 경험하지 않게 하고, 연결·자격증명·브라우저 프로필·등록은 backstage 설정으로 유지.
> ② **Action Window(실제 창 직접 행동) 프론트 정본 신설**(§18) + 리뷰 운영 화면(자동/Action Window/File
> Import/Integration Pending·마지막 성공·정체/실패·다운스트림 분석 완료). 근거: `docs/product-scope-v1.md`
> §1.2·§1.4·§1.5·§1.6, `docs/slices/action-window-v1.md`.
> 상위 계약은 `docs/product-scope-v1.md`(범위), 채널 capability 진실은
> `docs/multi-channel-connector-roadmap.md` §4.1(현행표)이 정본이며 본 문서는 이를 중복 선언하지
> 않는다. `docs/sellerops_ui_reference.md`의 유효 원칙(§1·§5·§6)은 본 문서로 흡수되었고, 그 문서는
> reference-only로 강등되었다.
>
> 표기 규칙: **[현행]** = 코드로 확인된 as-is. **[목표]** = 리디자인 방향. **[TODO-PO]** = 제품
> 오너 결정 대기. **[BE-DEP]** = 백엔드/API 선행 필요. 미확정 항목을 확정처럼 쓰지 않는다.

---

## 1. 목적과 범위

- 본 문서는 **Seller Track 프론트엔드**(`frontend/` — React 18 + TypeScript + Vite + Tailwind SPA)의
  리디자인 스펙이다. Manufacturer VOC Track은 장기 방향으로서 **혼입 금지**
  (`product-scope-v1.md` §3).
- 목표 인상: **여러 커머스 플랫폼을 하나로 합친 통합 셀러센터.** 기술 커넥터 콘솔이 아니라,
  셀러가 매일 여는 운영 화면.
- v1 기능 경계는 범위 문서를 따른다: **읽기·식별·우선순위까지**(채널 쓰기 없음), 데이터 다섯 종
  (주문·문의·리뷰·상품·운영 리포트).
- 본 문서가 다루는 것: 사용자·여정·IA·화면·상태 규칙·언어·접근성·슬라이스. 다루지 않는 것:
  수집 전략(커넥터 로드맵), 백엔드 설계, 시각 디자인 산출물(별도 작업).

## 2. 대상 사용자와 역할

중소 제조사/브랜드의 두 역할 (`product-scope-v1.md` §2):

| 역할 | 주 사용 맥락 | 주 화면 | 기기 |
|---|---|---|---|
| **오너(대표)** | 하루 현황 파악, 확인이 필요한 항목 훑기, 가벼운 검토/승인 | 대시보드, 알림, 고객 응대(읽기) | **모바일 비중 높음** |
| **온라인 판매 운영 담당자** | 문의·리뷰 응대 작업, 주문·매출 확인, 채널 연결·복구 | 고객 응대, 주문·매출, Backstage 전체 | 데스크톱 중심 |

- [현행] 기존 페르소나 문서(`sellerops_ui_reference.md` §1)는 "40–50대 제조사 CEO"를 명시했고
  타입 스케일(기본 17px, `tailwind.config.ts`)의 근거였다.
- [목표] 두 역할을 모두 지원하되 **큰 활자·저밀도 기조는 유지**한다(오너 경험 우선).
- [TODO-PO] 운영 담당자용 밀도 옵션(테이블 밀도 토글 등) 도입 여부.

## 3. Frontstage / Backstage

`product-scope-v1.md` §1.1의 2층 구조를 프론트 IA의 최상위 원리로 삼는다.

- **Frontstage — 매일의 커머스 운영(운영 에이전트 표면)**: 대시보드 / 주문·매출 / 고객 응대(문의+리뷰) /
  상품 이슈 / 리포트. **초점(2026-07-08)**: 에이전트 활동 · **완료된 자동 작업** · **사람 체크포인트** ·
  **긴급 운영 신호** · **실패/정체된 채널 연결** · **제안된 다음 행동** · **운영 결과**. 셀러가 제품을
  **커넥터-관리 콘솔로 경험하지 않게** 한다.
- **Backstage — 연결·설정**: 채널 연결, **자격증명, 브라우저 프로필, 등록 세부**, 동기화 설정, 수집 이력,
  기간 지정 수집, 파일 업로드, 연결 알림·복구. 연결·자격증명·프로필·등록은 **backstage 설정으로 유지**한다.
- 규칙:
  1. Frontstage가 1차 내비게이션이다. Backstage는 "설정/연결 관리" 성격의 2차 위계로 묶는다.
     > **2026-07-26 supersede.** "매일 여는 화면"이라는 기대는 대체된다 — 기본 UX는 **pull-first /
     > exception-push**(product-scope §1.8)다. Frontstage는 상시 응시 대상이 아니라, 미래엔 **확인할 일·진행
     > 중·완료 결과·연결 상태**만 있는 minimal Control Center로 수렴한다. FE는 상태·실행 순서를 소유하지 않는다.
  2. **기존 커넥터·수집 화면은 버리지 않고 Backstage로 재배치해 재사용**한다 — `/channels`,
     채널 상세(자동 수집 관리), `/upload`, `/alerts`는 온보딩·채널 관리·복구 플로우의 부품이 된다.
  3. Frontstage 화면에는 수집 파이프라인 용어(트리거, 커서, capability 등)가 노출되지 않는다(§12).
  4. Backstage 상태(연결 장애·수집 실패)는 Frontstage에 **요약 신호**(배지·배너)로만 올라오고,
     처리 동선은 Backstage로 이어진다.
  5. **운영 에이전트 프레이밍**: Frontstage는 "무엇이 자동으로 처리됐고, 지금 사람이 결정할 것은 무엇이며,
     어떤 운영 결과가 나왔는가"를 1차로 답한다(운영 루프 `product-scope-v1.md` §1.6). Action Window·File
     Import 같은 사람-개입 수집 작업은 **체크포인트로 노출**하되, 완료 후 다운스트림 자동 진행을 함께 보인다.

## 4. 사용자 여정

각 여정은 [현행] 상태와 [목표] 상태를 함께 기록한다. 근거 경로는 정찰 보고서(2026-07-07)와 코드.

### 4.1 가입과 워크스페이스 설정
- [현행] **가입 UI 없음.** `/login`에 데모 계정이 프리필(`frontend/src/pages/Login.tsx:8-9`).
  백엔드 `POST /api/auth/signup`(org+user 생성)은 존재하고 프론트 `api.signup`은 정의만 되고 미사용.
- [목표] 셀프서비스: **가입 → 워크스페이스/회사 설정(상호·담당자) → 채널 연결로 유도.**
  첫 파일럿은 assisted(운영자가 계정·연결을 대신 준비)를 허용하되, assisted 전용 화면을 따로
  만들지 않고 셀프서비스 플로우를 운영자가 대신 밟는 형태로 한다.
- [BE-DEP] 워크스페이스 설정 항목(조직 프로필 수정 API)은 현재 없음 — signup 파라미터(orgName)만 존재.
- 데모 프리필은 데모 모드(§13.4)에서만 유지하고 production 빌드에서 제거한다.

### 4.2 첫 채널 연결
- [현행] **Cafe24만 완결**: `/channels` 카드 → `/connect/cafe24`(몰 ID) → OAuth 동의 →
  `/connect/cafe24/result`. PENDING("연결 계속하기")·RECONNECT_REQUIRED("다시 연결하기") 재개 지원
  (`frontend/src/lib/channelConnection.ts`).
- [현행-결함] **비-Cafe24 API 채널은 막다른 길**: "연결하기" → 안내문만(`Channels.tsx` notice 분기).
  자격증명 입력 폼은 채널 상세(`/channels/:accountId`)에 있으나 **계정을 만들 UI/API가 없음**
  (`SellerAccountController`는 목록+파일채널 등록만).
- [목표] 채널 선택 → (채널별) **자동 연결(OAuth류) 또는 가이드형 키 등록**(발급처 안내 + 입력 +
  연결 확인) → 연결 완료. 연결 방식·가능 채널은 커넥터 로드맵 §4.1 현행표를 따르며, 현행표에
  없는 채널은 연결 카드를 노출하지 않거나 "미지원"으로 표기.
- [BE-DEP] **비-Cafe24 채널의 seller-account 생성 엔드포인트 신설 필요** — 이 여정의 최대 선행 조건.

### 4.3 첫 동기화
- [현행] 채널 상세의 "지금 수집하기"(수동 동기화)·"기간 지정 수집"(백필)·업로드가 존재. 결과는
  행 수 카운트로 보고.
- [목표] 연결 직후 **첫 동기화를 온보딩 단계로 안내**(무엇을 얼마나 가져왔는지 셀러 언어로 요약)
  → 스케줄·알림 설정 제안 → 대시보드 착지. `totalRows=0`은 "최신 상태(변경 없음)"로 표기(실패 아님).

### 4.4 일상 운영 (매일)
- [목표 루프] 대시보드에서 하루 요약과 "확인 필요" 항목 확인 → 고객 응대에서 문의·리뷰 처리
  (v1: 확인·분류·응답 유형 제안까지) → 주문·매출 확인 → (필요 시) 상품 이슈 검토.
  모바일에서는 이 루프의 읽기·확인 부분이 1급(§14).
- [현행 격차] 홈이 주문·매출만 표시(문의·리뷰 수치는 라이브 소스 부재로 의도적 제외 —
  `Home.tsx:10-14`). 문의·리뷰가 인박스/문의 응답/채널 상세에 분산.

### 4.5 연결 장애와 복구
- [현행] 잘 갖춰진 편: 연결 알림(`/alerts` — 유형별 안내+확인 처리), 채널 상세의 상태별 "다음 조치"
  패널(6개 상태, `ChannelDetail.tsx:115-154`), 연결 확인(auth 테스트), 자격증명 재입력, 수집 재시도.
- [목표] ① Frontstage에 장애 요약 신호(대시보드 배너·내비 배지 [현행 유지]) → ② 알림에서 원클릭으로
  해당 채널의 복구 동선 진입 [현행 유지] → ③ 복구 완료를 같은 화면에서 확인. Cafe24 외 채널의
  "다시 연결" 동선은 4.2의 [BE-DEP] 해소 후 일반화.

### 4.6 데이터 없음·부분 데이터 상태
- [현행] 빈 상태는 대체로 정직("아직 업로드된 문의/리뷰가 없습니다" + 업로드 CTA). 오류 카피가
  개발자 언어("백엔드가 실행 중인지 확인해 주세요")로 통일돼 있는 것이 문제(§13).
- [목표] 3분류를 구분해 표기한다:
  - **아직 연결 안 됨** → 채널 연결 CTA (온보딩 유도).
  - **연결됨, 데이터 0건** → "최신 상태" 또는 "수집된 데이터가 아직 없습니다" (실패 아님).
  - **일부 채널만 연결/일부 기간만 수집** → 집계 화면에 **부분 데이터 고지**(예: "네이버 기준,
    다른 채널은 연결되지 않았습니다") — 전체인 것처럼 보이는 부분 집계 금지.

## 5. 목표 정보 구조 (IA)

```
Frontstage (1차 내비게이션)
├─ 대시보드            오늘의 운영 현황 + 확인 필요 항목 (org 횡단)
├─ 주문·매출           기간·채널 필터 집계 (현행 /orders 계승)
├─ 고객 응대           문의 + 리뷰 통합 워크스페이스 (현행 /inbox + /inquiries 통합)
├─ 상품 이슈           상품별 반복 이슈 후보 (현행 /issues 계승)
└─ 리포트              수집 데이터 기반 리포트 (데이터 생기기 전까지 정직한 빈 워크스페이스)

Backstage (2차 위계 — "연결·설정")
├─ 채널 연결           카탈로그 + 연결 상태 (현행 /channels)
│   └─ 채널 상세       연결 정보·동기화 설정·수집 이력·기간 지정 수집 (현행 /channels/:accountId)
├─ 연결 알림           커넥터 알림 + 확인 처리 (현행 /alerts)
├─ 자료 업로드         파일 업로드 브리지 (현행 /upload — 1차 메뉴에서 이동)
└─ 워크스페이스 설정    조직·계정 (신규, [BE-DEP])

제외
└─ AI 검색             standalone 페이지 제거 — 향후 맥락형 "운영 메모리" 패널로만
                       (scope §5·§7-10, 별도 승인 전 구현 금지)
```

- 고객 응대 통합의 근거: 같은 데이터(문의·리뷰)가 현재 4곳(인박스, 문의 응답, 채널 상세의
  수집된 리뷰·문의, 상품 이슈)에 다른 형태로 등장해 정본 화면이 없다(정찰 F3). 통합 워크스페이스가
  정본이 되고, 채널 상세의 목록은 "이 채널에서 수집된 것" 확인용 보조 뷰로 유지한다.
- [TODO-PO] 카탈로그 노출 채널 집합 — 현행 데모 카탈로그 13채널(`frontend/src/lib/mocks.ts:76-95`)
  vs 커넥터 문서 채널 7종. v1 카드 노출 기준(연결 가능만? 요청하기 카드 존치?) 결정 필요.

## 6. 내비게이션과 모바일 내비게이션

- [현행] 데스크톱 좌측 사이드바 10개 메뉴(`Sidebar.tsx`), **`md` 미만에서 사이드바가 사라지고 대체
  내비게이션 없음** — 모바일 탐색 불가(정찰 F2, Critical).
- [목표-데스크톱] 사이드바 2그룹: Frontstage 5개 + "연결·설정" 그룹(Backstage). 연결 알림 배지는
  현행 로직(`openAlerts.tsx`) 유지.
- [목표-모바일] **하단 탭 바**: 대시보드 / 주문 / 고객 응대 / 알림 (+ "더보기"에 Backstage).
  Backstage 밀도 화면(채널 상세, 업로드)은 데스크톱 우선 — 모바일에서는 열람 가능하되 최적화
  후순위(scope §5 모바일 1급 범위).
- 이모지 아이콘은 아이콘 세트로 교체 [목표 — 시각 폴리시 슬라이스에서].

## 7. 현행 → 목표 라우트 매핑

| 현행 라우트 | 현행 화면 | 목표 | 비고 |
|---|---|---|---|
| `/login` | 로그인 | **유지 + 가입 진입 추가** | 데모 프리필은 데모 모드 전용으로 |
| — | (없음) | **신설: 가입·워크스페이스 설정·온보딩** | [BE-DEP] 4.1·4.2 |
| `/` | 홈(주문·매출만) | **재설계: 통합 대시보드** | §9, [BE-DEP] org 횡단 집계 |
| `/orders` | 주문·매출 | 유지(경로·기능 계승, 카피 정리) | 주문 "목록"은 데이터 부재 — [BE-DEP] |
| `/inbox` | 인박스 | **`/inquiries`와 통합 → 고객 응대** | §10 |
| `/inquiries` | 문의 응답 | 〃 | 제안 생성 워크플로 계승 |
| `/issues` | 상품 이슈 | 유지 | 분석 실행 트리거 부재는 [BE-DEP] |
| `/reports` | 리포트(빈 워크스페이스) | 유지(정직한 빈 상태), 데이터 연동은 후순위 | |
| `/search` | AI 검색(placeholder) | **제거** — 내비·라우트에서 삭제 | scope §7-10 |
| `/channels` | 채널 연결 | **Backstage로 이동**(기능 유지) | |
| `/channels/:accountId` | 자동 수집 관리 | Backstage 유지 + 섹션 위계 재정렬 | §11 |
| `/connect/cafe24`, `/connect/cafe24/result` | Cafe24 연결 | 유지, 온보딩 플로우에 편입 | |
| `/upload` | 자료 업로드 | **Backstage로 이동**(1차 메뉴에서 제외) | 업로드=브리지(커넥터 로드맵 §5) |
| `/alerts` | 연결 알림 | Backstage 유지 | [TODO-PO] 일반 알림 센터로 확장 여부 |
| `*` | `/`로 리다이렉트 | 404 화면 신설 | |

## 8. 화면 처분표 (keep / move / redesign / hide / remove)

| 처분 | 대상 | 근거 |
|---|---|---|
| **keep** | 주문·매출, 상품 이슈, 리포트(빈 상태), Cafe24 연결 2종, 연결 알림 | 방향과 정합, 카피만 정리 |
| **move** | 채널 연결·채널 상세·자료 업로드 → Backstage | frontstage/backstage 원리 |
| **redesign** | 홈 → 통합 대시보드; 인박스+문의 응답 → 고객 응대 워크스페이스; 로그인(+가입) | §9·§10·4.1 |
| **hide** | 데모 전용 장치(로그인 프리필, mock 폴백 카탈로그) → 데모 모드 뒤로 | §13.4 |
| **remove** | AI 검색 페이지, 미사용 API 표면(`api.signup`는 가입 신설 시 사용, `getDashboardSummary`·`getChannelStatus`·비-strict `getInbox`·`registerFileChannel`은 정리 검토) | scope §7-10, 정찰 |

## 9. 대시보드 원칙

1. **"오늘 무엇을 확인해야 하는가 + 에이전트가 무엇을 이미 처리했는가"가 첫 화면의 답**이다. 확인 필요
   항목(미답변 문의, 부정 리뷰, 연결 장애, 주문 급변)과 **사람 체크포인트**(Action Window·File Import 대기
   작업)가 지표 카드보다 위이거나 동급 최상단. 함께 **완료된 자동 작업**(수집·정규화·분석 완료)·**긴급 운영
   신호**·**실패/정체된 채널 연결**·**제안된 다음 행동**·**운영 결과**를 표면화한다(§3 규칙 5, 운영 루프
   `product-scope-v1.md` §1.6). 커넥터 관리 화면이 아니라 **운영 에이전트 활동 화면**으로 읽혀야 한다.
2. **정직한 수치 사다리**: 라이브 소스가 있는 지표만 수치로 올린다(현행 `Home.tsx` 원칙 계승).
   소스가 없으면 카드 자체를 만들지 않는다 — "0" 또는 가짜 시드로 채우지 않는다.
   지표별 편입 시점은 커넥터 현행표의 상태를 따른다.
3. **부분 데이터 고지**: 연결된 채널 기준임을 명시(4.6). "네이버 주문·매출이 연결되어 있습니다"
   같은 하드코딩 문구([현행] `Home.tsx:23`)는 연결 상태 데이터에서 파생한 동적 문구로 교체.
4. **org 횡단이 기본 단위** [BE-DEP]: 현행 attention/요약 API는 계정 단위
   (`/api/seller-accounts/{id}/…`) — 대시보드가 채널 횡단이 되려면 org 수준 집계 엔드포인트 필요.
   그 전까지는 "채널별 카드 나열"로 정직하게 구성.
5. 모바일에서 대시보드는 **읽기 전용 요약 + 알림 진입**으로 완결돼야 한다(§14).

## 10. 고객 응대 워크스페이스 원칙

1. **문의·리뷰의 단일 정본 화면.** 유형(문의/리뷰)·채널·상태(미답변/부정)·처리할 작업 필터를
   하나의 워크스페이스로 통합. 현행 인박스의 필터·분석 칩과 문의 응답의 큐→상세→제안 워크플로를
   합친다.
2. **v1 능력의 정직한 한계 유지**: **발송은 없다. 발송처럼 보이는 버튼 금지.** 이 규정은 v1.4 이후에도
   그대로다 — 바뀐 것은 초안의 유무이지 outbound 경계가 아니다.
   - **문의(inquiry)**: 응답 **유형 제안까지**이며 답변 초안·발송은 없다(현행 카피의 정직성 계승 —
     `Inquiries.tsx` ProposalCard 고지 유지).
   - **리뷰(review) — v1.4 변경**: `RESPONSE_NEEDED` 리뷰에 한해 **답변 준비**를 노출한다(product-scope
     §5) — 규칙 기반 추천 초안 → 편집 → 승인 → **복사**. 승인은 텍스트를 **고정**할 뿐 전송하지 않고,
     답변은 **운영자가 직접 채널에 붙여넣는다**. 카피는 그 사실을 숨기지 않는다(예: 복사 후
     "복사했습니다. 채널에 직접 붙여넣으세요"). **"발송"·"전송"·"등록" 라벨 금지**, 발송을 암시하는
     진행 표시·성공 토스트 금지.
   - **리뷰(review) — v1.6 변경(가이드형 제출)**: 승인된 답변에 한해 체인을
     `… → 복사 → (선택) 네이버에서 직접 답변하기(가이드) → 로컬 결과 기록`으로 확장한다(product-scope §9,
     NAVER 전용). **여전히 발송은 없다** — SellerOps는 창을 앞으로 가져오고 답변란을 관찰만 하며,
     **판매자가 직접 붙여넣고 제출**한다. **"발송"·"전송"·"등록" 라벨 금지**는 이 흐름에도 그대로 적용된다
     (가이드 버튼은 "네이버에서 직접 답변하기", 결과 버튼은 "답변함으로 기록"/"답변 안 함으로 기록" 처럼
     **기록** 어휘를 쓴다). 결과는 **운영자 보고 + 검증 두 사실을 항상 쌍으로** 표기하고(예: "채널에 직접
     답변한 것으로 기록했어요 · 확인 안 함"), **`확인 안 함(UNVERIFIED)`을 단독 표기하지 않으며 "완료"도
     쓰지 않는다**(read-back 없음 → 게시 여부 미검증). §18.3의 자동 클립보드·페이지-비밀 추출 금지는 유지
     — 운영자가 복사·붙여넣기를 직접 수행한다.
   - 두 표면이 다른 이유: 리뷰에는 초안이 있고 문의에는 없다. 문의 답변 워크플로 노출은 여전히 별도
     결정이다(아래 4).
3. **분석·추천 표기(2026-07-26 갱신).** 분석 신호 표기는 여전히 과장하지 않는다(현행 `InboxFeed.tsx:124`
   원칙 유지 — FE가 라벨을 소유). **리뷰·문의 답변 초안은 Company Voice 기반 AI 생성**이다(product-scope §5.2)
   — ~~"규칙 기반·라이브 LLM 없음"~~ 잠금은 대체된다. 그러나 **정직성 경계는 그대로**: 초안은 발송이 아니고
   (§10.2·§15·§18), 승인은 텍스트를 고정할 뿐이며, 실제 발송은 Capability 지원 + 승인일 때만, "발송/전송/등록"
   라벨과 발송 암시 표시는 금지, 검증 불가 채널은 `UNVERIFIED`를 쌍으로 표기한다. FE는 초안이 사실을 지어내지
   않도록(사실은 Product Knowledge·운영자 확인) 표기·경고를 소유한다.
4. [BE-DEP] 인박스 페이징(`GET /api/inbox` 무페이징), 문의 답변 워크플로(백엔드
   draft/verify/confirm-publish는 존재하나 미연결 — 노출 시점은 별도 결정).
   - ~~리뷰 본문 노출 정책~~ → **v1.4에서 확정**: 답변 준비 화면은 **redacted 리뷰 본문**을 노출한다
     (`VocPreviewSanitizer` 전체 길이 — 60자 `safePreview` 아님, 원문 아님). attention **목록**은 계속
     metadata-only. 근거·경계는 product-scope §9의 좁은 예외. **Cafe24 아티클 API는 여전히 메타데이터
     전용**이므로 답변 준비 대상이 아니다(앵커 없음 → `actionRef` null → 어피던스 자체가 없음).
5. [TODO-PO] 메뉴명 확정 — "고객 응대" vs "문의·리뷰" vs 기타. "문의 응답"(현행)은 발송 기대를
   유발하므로 지양.

## 11. 채널 관리(Backstage) 원칙

1. **채널 상세의 섹션 위계 재정렬**: 연결 상태·다음 조치(최상단) → 연결 정보 → 동기화 설정 →
   수집 이력 → 기간 지정 수집(고급) → 수집된 데이터 확인. [현행] "기간 지정 수집"이 "연결 상태"
   위에 있음(정찰 F7).
2. **상태 어휘 단일화**: 카탈로그 status / 계정 connectionStatus / 연결 state / run status가 셀러에게
   서로 다른 배지로 보이는 현행(정찰 F11)을 "이 채널은 지금 정상인가 + 마지막 동기화"의 한 문장
   요약으로 통합하고, 세부는 펼침으로.
3. **capability 표기는 현행표 위임**: "수집 가능 데이터" 배지·문구는 커넥터 로드맵 §4.1의 셀러 표기
   열과 4단계 규칙(부록 A — 운영 지원만 "지원")을 따른다. 프론트에 별도 진실 목록을 두지 않는다.
4. 연결 실패·만료 상태에서는 **다음 조치 1개**가 화면의 주인공(현행 NextActionPanel 계승).
5. 업로드는 "자동 연결이 어려울 때의 백업"이라는 현행 프레이밍 유지.

## 12. 셀러 언어 사전 (개발자 용어 → 셀러 용어)

프론트 카피 전면에 적용. 금지어 검사는 기존 카피 테스트 패턴(`pages-copy.test.ts`,
`channelSupport.ts BANNED_SUPPORT_PHRASES`)을 확장해 강제한다.

| 금지(개발자/내부 용어) | 사용(셀러 용어) |
|---|---|
| "백엔드가 실행 중인지 확인해 주세요" | "일시적인 문제로 불러오지 못했습니다. 잠시 후 다시 시도해 주세요." (+재시도 버튼) |
| capability, 스캐폴드, 커넥터 | 수집 가능 데이터, 연결 방식 |
| strict/mock/시드 | (노출 금지 — 데모 모드 라벨은 "데모 데이터"로) |
| 백필 | 기간 지정 수집 [현행 용어 유지] |
| 트리거/커서/run | 실행 방식(자동/수동/재시도/업로드) [현행 칩 유지], 수집 이력 |
| API 키/JWT/HMAC 등 인증 기술명 | "연결 정보" + 발급처 안내 [현행 방향 유지] |
| 로드맵 문구("다음 단계에서 제공" 등) | (금지 — 미지원은 미표기 또는 "미지원") |
| RECONNECT_REQUIRED 등 enum 원문 | 상태별 한국어 라벨 [현행 매핑 유지·보완] |

동기화 용어: 셀러 대면은 **"동기화"** 를 기본으로, "수집"은 데이터 관점 서술에 허용(혼용 시 문장
단위로 일관). [TODO-PO] 최종 확정.

## 13. 상태 규칙 (로딩·빈·미지원·부분·오류)

1. **로딩**: 화면 골격 유지 + "불러오는 중…"(현행 패턴 유지). 8초 타임아웃(axios) 초과 시 오류 상태로.
2. **빈 상태**: 4.6의 3분류(미연결/0건/부분)를 구분. 빈 상태에는 항상 다음 행동 CTA 1개.
3. **미지원**: capability 부재는 "이 채널 미지원" + 대안(업로드) 안내(현행 ScheduleRow 패턴 유지).
   로드맵 문구 금지.
4. **데모/개발 모드 분리** (제품 결정): mock/시드 데이터는 **명시적 데모 모드에서만** 렌더된다.
   - [현행] `VITE_USE_MOCKS`(명시적) 외에 `getOrMock`의 **조용한 폴백**이 잔존(채널 카탈로그·스케줄·
     업로드 채널 목록 — `apiClient.ts:88-98`).
   - [목표] production 빌드에서 조용한 폴백 제거 — 모든 읽기를 fail-closed로 통일. 데모 모드에서는
     화면에 "데모 데이터" 표시를 상시 노출.
5. **오류**: 셀러 언어(§12) + 재시도. 부분 실패(채널 목록만 실패 등)는 화면 전체를 죽이지 않고
   해당 블록만 강등하되 강등 사실을 표기(현행 Orders 채널 필터 패턴).
6. **행동 피드백**: 결과 메시지는 행동한 요소 근처(인라인) 우선, 페이지 상단 배너 단독 사용 지양
   (정찰 F9). 전역 토스트 도입 여부는 구현 슬라이스에서 결정.

## 14. 반응형·접근성 원칙

(`sellerops_ui_reference.md` §5·§6에서 흡수·계승)

- **모바일 1급 범위**: 하루 개요·알림·문의·리뷰 열람·가벼운 검토/승인. Backstage 밀도 도구는
  데스크톱 우선. 하단 탭 내비(§6) 필수 — 사이드바 소실 상태로 출시 금지.
- 큰 활자(기본 17px 스케일 유지), WCAG AA 대비, **색+라벨 병용**(색 단독 의미 금지 — 별점
  `★repeat` 등 스크린리더 무의미 패턴 보완), 큰 히트 타깃, 키보드 내비게이션·focus-visible,
  상세 패널 열림 시 포커스 이동, 아이콘에 `aria-hidden`/라벨.
- 모션: 의미 있는 상태 변화에만, ≤ ~200–250ms, `prefers-reduced-motion` 존중. 장식 모션 금지.
- 한국어 우선, 빈 화면 금지(항상 EmptyState).

## 15. Capability 정직성 규칙

- 채널·데이터 지원 표기의 진실은 **커넥터 로드맵 §4.1 현행표**와 부록 A의 4단계 모델
  (연결 가능/구현됨/라이브 검증/운영 지원)이다. **셀러에게 "지원"으로 보이는 표기는 운영 지원
  단계만** 허용.
- 프론트는 자체 지원 목록을 하드코딩하지 않고 백엔드 capability API + `channelSupport.ts`류의
  보수적 문구 계층으로 표기한다. 문구 규칙 위반은 카피 테스트로 차단.
- 미래·가설 기능(답변 **발송**, 운영 메모리 패널, 미검증 채널 자동 수집)은 UI에 어떤 형태로도
  선표기하지 않는다. **v1.4 주의**: 리뷰 답변 **준비**가 생겼다고 해서 발송이 예고된 것이 아니다 —
  "곧 발송 가능"·"발송 준비 중" 류의 표기는 여전히 금지이며, 준비 화면은 발송이 없다는 사실을
  숨기지도 예고하지도 않는다(§10.2).

## 16. 가이드 연결 (Guided Connection) — 정본

채널 연결을 비기술 오너·운영자가 스스로 완주하도록 **단계별로 안내**하는 경험. 제품 원칙은
`docs/product-scope-v1.md` §1.2·§6.1, 런타임 경계는 `docs/sellerops_local_agent_runtime_adr.md`
(Runtime ADR)가 정본이며, 본 절은 그 위의 **프론트 화면·상호작용 정본**이다.

> **정직성 경계.** 이 절이 기술하는 목표 경험 중 **인앱 브라우저 프로젝션·자동 재로그인·OS
> 자격증명 저장·Windows 지원은 아직 구현되지 않았다.** 화면·문구로 구현된 것처럼 보이게 하지 않는다.
> 시각 세부(패널 폭, 버튼 라벨, 애니메이션, 코치마크 모양)는 **[UX-DECISION]** — 본 문서에서 확정하지
> 않는다.

### 16.1 목표 온보딩 경험
- 채널 선택 → SellerOps가 해당 채널의 실제 연결/발급 화면을 안내하며 **게임 튜토리얼처럼 단계별로**
  진행 → 필요한 사용자 행동(로그인·발급 클릭 등)을 짚어주고, 안전한 편의 단계는 자동 수행 →
  연결 확인 → 첫 동기화 → 대시보드 착지.
- 목표 형태는 실제 마켓/API-센터 브라우저 흐름을 **SellerOps 안에서** 보여주는 것(프로젝션). 파일럿은
  더 단순한 형태(별도 브라우저 창 + 옆의 단계 안내)로 시작할 수 있으나, **아키텍처는 인앱 프로젝션으로
  확장 가능해야 한다**(Runtime ADR).

### 16.2 프로젝션 레이아웃 (구조 원칙만; 치수는 [UX-DECISION])
- 2영역 구조: **프로젝션 뷰**(실제 브라우저 화면 투사) + **단계 패널**(현재 단계·진행·다음 행동).
- 프로젝션 뷰 위에 타깃 하이라이트(코치마크)를 얹을 수 있다 — 단, 좌표·요소 메타데이터를 프론트로
  넘기는 것은 sanitized 계약의 **로컬 신뢰 채널 예외**가 선결이다(§16.9, Runtime ADR, [PO-DECISION]).
- 파일럿 단계에서 프로젝션이 없으면 단계 패널만으로 별도 창을 안내한다(동일 단계 정의 재사용).

### 16.3 단계 패널·진행 동작
- 각 단계: 제목 · 짧은 설명 · "지금 할 일"(사용자 행동) 또는 "SellerOps가 진행 중"(자동) 표시 ·
  완료 조건. 진행 표시(예: n/총 단계)는 구조 요구이며 **표현 방식은 [UX-DECISION]**.
- 단계 정의는 **채널별 재사용 가능한 flow 정의**에서 온다(다채널 확장 대비 — Connector Roadmap
  "가이드 셋업 방식" 열, Runtime ADR의 flow 정의 경계).

### 16.4 사용자 행동 vs SellerOps 행동 규칙
- **SellerOps 자동(안전·결정적 편의 단계)**: 페이지 열기·이동, 입력란으로 스크롤, 붙여넣기 위치 안내,
  연결 테스트 실행, 다음 단계로 진행.
- **사용자 통제(항상)**: 계정/스토어 선택, 권한·동의, 발급 클릭, 법적 의미 있는 결정, 의도가
  불확실한 판단. 감독형 클릭이 필요한 경우 **정확히 1개**만, 사용자 승인 후(현행 감독형 클릭 원칙
  계승 — collector `review-usage-confirm.ts`).

### 16.5 로그인·2FA·CAPTCHA 처리
- **절대 자동화/우회하지 않는다.** 인증이 필요하면 단계 패널이 "직접 로그인/인증해 주세요"로 사람에게
  넘기고, 완료를 감지하면 다음 단계로 진행한다. 에이전트는 자격증명을 입력하지 않는다.

### 16.6 실패·복구·재개
- 단계 실패(요소 못 찾음·페이지 변경·타임아웃)는 **해당 단계에서 멈추고** 재시도/직접 진행 안내를
  제시한다(무한 자동 재시도 금지).
- 세션 만료·계정 드리프트 시: 전용 프로필 세션 보존 + 사람 재로그인 요청(§16.7 폴백). 중단된 작업은
  재개 가능해야 한다(자동 재로그인이 붙으면 이어서, 아니면 사람 개입 후).

### 16.7 로컬 에이전트 상태 (offline / 미설치 / 미페어링)
가이드 연결은 로컬 에이전트를 전제로 하므로, 프론트는 아래 상태를 **명시적으로** 표시한다:
- **미설치**: 로컬 에이전트 설치 안내(설치 패키지·절차는 [BE/AGENT-DEP], 현재 미구현).
- **미페어링**: 프론트-에이전트 페어링 유도(페어링 계약은 Runtime ADR, 미구현).
- **offline(설치·페어링됨, 실행 안 됨)**: "로컬 에이전트가 실행 중이 아닙니다" + 실행 안내.
- **정상(페어링+실행)**: 실시간 단계 상태 수신.
> 이 상태들의 이벤트/전송 채널은 **현재 미구현**(정찰 §3: 프론트-에이전트 통신 부재). 화면은 상태
> 개념을 정의하되, 실동작은 Local Agent Bridge 슬라이스(§17-B G1)에 의존한다.

### 16.8 반응형
- 가이드 연결(프로젝션+발급 흐름)은 **데스크톱 우선**(로컬 에이전트가 데스크톱 런타임). 모바일에서는
  진행 현황·완료 확인 정도의 열람만 지원하고, 실제 연결 작업은 데스크톱으로 안내(§14 모바일 범위와 정합).

### 16.9 보안·프라이버시 규칙
- **영속 로그 금지 대상**(Browser Projection 원칙 계승): 스크린샷·프레임·키 입력·URL·자격증명·
  페이지 본문을 영속 로그에 남기지 않는다. collector의 sanitized 계약(enum/boolean/coarse bucket/
  16-hex 해시)과 일치.
- 프로젝션 스트림·입력 릴레이는 **로컬 신뢰 채널** 전제(전송·비영속·비로깅). 좌표/URL/요소
  메타데이터를 프론트로 넘기는 예외는 **명시적 정책 결정** 후에만(§16.2, [PO-DECISION]).
- 동의 3종(자동 로그인 · 자격증명 저장 · 마켓 권한)은 **분리된 명시적 동의**로 각각 받는다
  (`product-scope-v1.md` §1.2). Device Vault·자동 입력은 미구현 — UI가 있는 것처럼 표기 금지.

### 16.10 NAVER 프로토타입 흐름과 완료 기준
- **첫 가이드 연결 프로토타입: NAVER 커머스 API 애플리케이션 발급 + 연결.**
- **파일럿 운영 모델**: assisted 온보딩(제품 오너가 관찰하는 가운데 진행).
- **완료 기준(6단계)**: ① SellerOps에서 NAVER API 센터 흐름 열기 → ② 앱 발급을 사용자에게 안내 →
  ③ Client ID·Secret을 안전하게 등록 → ④ 연결 테스트 통과 → ⑤ 첫 실주문 데이터 수집 → ⑥ SellerOps에
  수집 결과 표시.
- **현행 근거**: 주문 수집은 `POST …/credentials`(Vault 저장) → `test-connection` → 수동 sync →
  대시보드로 이미 존재(NAVER ORDER_SUMMARY 라이브 1회 검증 — Connector Roadmap §4.1). 가이드 연결은
  이 경로에 **단계 안내·in-app 발급 화면**을 얹는 것이다.
- **경계**: 이 흐름은 **셀러 소유 NAVER 앱 발급 파일럿 경로**다. 미래 SellerOps **솔루션-제공자
  OAuth 연동 모델로 문서화하지 않는다**(`product-scope-v1.md` §6.1).

### 16.11 첫 파일럿 범위와 명시적 제외
- **범위**: NAVER 6단계 완료 기준(§16.10)을 assisted로 1회 통과.
- **제외**: 인앱 프로젝션 없이도 파일럿 가능(별도 창 + 단계 패널); 자동 재로그인·Device Vault·
  Windows·클라우드·다채널 flow 자동화·자동 발급 클릭은 **제외**. 인증 우회·계정 선택 자동화 **영구 제외**.

## 17. 구현 슬라이스와 FE/BE 의존성

작은 PR 규율(CLAUDE.md) 준수. 순서는 권장이며 각 슬라이스는 독립 머지 가능해야 한다.
**본 문서는 계획이다 — 코드 착수는 슬라이스별 승인으로.** Product Shell(§17-A)과 가이드 연결
인프라(§17-B)는 **분리된 트랙**이며, 승인된 시퀀싱은 아래를 따른다.

### 17-A. Product Shell 트랙 (프론트 IA·화면 — 새 백엔드/에이전트 능력 불필요 우선)

| # | 슬라이스 | 내용 | 의존성 |
|---|---|---|---|
| S1 | 오류 카피·셀러 언어 교체 | §12 사전 적용 + 카피 가드 테스트 확장 | FE only |
| S2 | 모바일 내비게이션 | 하단 탭 + Backstage 접근(§6) — 현행 화면 그대로 | FE only |
| S3 | IA 재편(통합 셀러센터) | 사이드바 2그룹화, 대시보드=frontstage, 채널·수집 도구 Backstage 이동, `/search` 제거, 404 | FE only |
| S4 | 데모 모드 분리 | `getOrMock` 조용한 폴백 제거, 데모 모드 표시(§13.4) | FE only |
| S5 | 고객 응대 워크스페이스 | `/inbox`+`/inquiries` 통합(§10) | FE 위주; 페이징은 [BE-DEP] |
| S6 | 대시보드 재설계 | §9 — 채널별 카드 구성부터, org 집계는 BE 후 | [BE-DEP] org 횡단 집계 |
| S7 | 온보딩·가입 | 가입→워크스페이스→채널 연결→첫 동기화(§4.1–4.3) | [BE-DEP] 조직 설정·비-Cafe24 계정 생성 신설 |
| S8 | 채널 연결 일반화 | 비-Cafe24 연결 dead-end 해소(§4.2) | [BE-DEP] seller-account 생성 API |
| S9 | 채널 상세 재정렬·상태 어휘 통합 | §11 | FE only |
| S10 | 시각 폴리시 | 아이콘 세트, 접근성 보완(§14), 토스트 결정 | FE only |

### 17-B. 가이드 연결 인프라 트랙 (로컬 에이전트·프로젝션 — Runtime ADR 경계)

승인된 시퀀싱(Runtime ADR와 정합). **Product Shell과 독립적으로 진행**하며, 각 단계는 별도 승인.

| # | 슬라이스 | 내용 | 의존성 |
|---|---|---|---|
| G1 | Local Agent Bridge | 프론트-에이전트 안전 페어링, 로컬 실시간 상태 이벤트, 브라우저/세션 수명 보고, 웹 UI와 독립된 에이전트 수명 | [AGENT-DEP] 현재 통신 채널 부재(정찰 §3) |
| G2 | Browser Projection V0 | 실제 Chrome 뷰를 SellerOps에 투사, 마우스/키보드 릴레이, §16.9 영속 로그 금지, 연결·복구 동작 | G1; [PO-DECISION] sanitized 좌표/URL 예외 |
| G3 | NAVER Guided Connection | 단계 정의, 사용자 통제 vs 자동 경계, 안전한 자격증명 등록, 연결 테스트, 첫 주문 수집(§16.10) | G1·G2 |
| G4 | 자동 재로그인 | 명시적 동의, macOS 자격증명-vault 어댑터(파일럿), Windows 어댑터(후속), 사람 개입 폴백, 중단 작업 재개 | G3; [AGENT-DEP] Device Vault 미구현 |
| G5 | 튜토리얼 강화 | 타깃 하이라이트, 클릭 검증, 자동 이동, UI 드리프트 복구, 재사용 다채널 flow 정의 | G3 |
| G6 | Windows 마이그레이션 | Windows Chrome 런타임, Windows 자격증명 저장소, 자동시작/백그라운드, 패키징·설치·업데이트·회사 PC 검증 | G1–G5; OS 어댑터(Runtime ADR) |

> **Action Window 트랙(2026-07-08 신설, G2와 구분).** **라이브 마켓 리뷰 수집의 기본 production 모드는
> Action Window**(실제 창 직접 행동 + 오버레이, §18)이며, **G2 Browser Projection은 비-기본 렌더러**로
> 유지된다(제거·폐기 아님). Action Window 슬라이스(AW-1 계약 리뷰 → AW-2 공통 엔진+합성 → AW-3 ESM+ 어댑터
> → AW-4 ESM+ 라이브 보정(별도 승인) → AW-5 채널 확장)는 `docs/slices/action-window-v1.md`가 정본이며,
> **G3 가이드 상태 엔진을 공유**한다(마켓 로직 중복 금지). "Projected Direct Action"은 채널별 정책·제품
> 리뷰 후 이후에 활성화될 수 있다.

이연(별도 승인): 리포트 데이터 연동, 운영 메모리 패널, **문의** 답변 워크플로 노출, 주문 목록/상세,
클라우드 관리형 런타임.
- **리뷰 답변 준비는 이연 목록에서 빠졌다** (product-scope v1.4, 2026-07-17 제품 오너 결정) — 백엔드는
  구현·검증 완료, FE 노출은 별도 슬라이스(`docs/slices/review-response-preparation-v1.md`).
  **문의** 답변 워크플로(draft/verify/confirm-publish)는 발송 경로를 포함하므로 **이연 유지**.

### 17.1 API 계약 방침

- [현행] `frontend/src/lib/types.ts`(백엔드 DTO 수기 미러)와 `apiClient.ts`가 사실상의 FE 계약이다.
  이는 **임시 미러이며 장기 source of truth가 아니다.**
- [목표] 계약의 정본은 **백엔드 OpenAPI 명세**로 한다. 프론트 타입과 mock은 그 계약에서
  **생성하거나 계약에 대해 검증**하는 구조로 이행한다(springdoc 등 도입은 별도 BE 슬라이스).
- 이행 전까지의 규칙: DTO 변경은 백엔드 PR에서 `types.ts` 동기 수정을 동반하고, 신규 화면은
  `types.ts`에 없는 필드를 임의 가정하지 않는다.

---

## 18. Action Window · 리뷰 운영 화면 (프론트 정본, 미구현)

**Action Window = 라이브 마켓 리뷰 수집의 기본 production 모드**(제품 결정 `docs/product-scope-v1.md`
§1.5, 계약 `docs/slices/action-window-v1.md`). **2026-07-26 정정:** Action Window는 **NAVER 리뷰 한정으로
라이브 검증**됐으나(product-scope §1.5·§4.1 — 1계정·disposable·**운영 지원 아님**), 범용 렌더러·그 외 채널은
미구현이다 — 화면·문구로 셀러에게 **"상시 제공"**으로 보이게 하지 않는다. 본 절은 그 위의 **프론트 화면·상호작용 정본**이며 §16(가이드 연결)과
같은 정직성 경계·셀러 언어를 계승한다. **시각 세부(스포트라이트 모양·색·애니메이션·문구)는
[UX-DECISION]** — 확정하지 않는다.

> **정직성 경계.** 실제 마켓 페이지 위 오버레이·다운로드 완료 감지는 **미구현**이다. 화면·문구로 구현·마켓
> 승인된 것처럼 보이게 하지 않는다. 실제 마켓 사용은 **정책 게이트** 뒤에 있다(계약 §15·§17).

### 18.1 Action Window 필수 UX
실제 마켓 창을 사용자가 직접 조작하며, SellerOps는 그 위에 안내 오버레이를 얹는다. 필수 요소:
- **올바른 Chrome 탭/창을 앞으로 가져오기**(bring-to-front) · **현재 단계 / 총 단계** · **실제 대상 위
  스포트라이트/배지** · **간결한 지시** · **안내 on/off** · **현재 단계 다시 찾기** · **수동 모드** ·
  **일시정지·재개** · **"이 단계를 완료했어요" 폴백** · **recoverable UI-drift 상태**(마켓 UI 변경 시
  0행동 + 사용자 확인) · **다운로드 완료 감지 표시** · **SellerOps로 안전 핸드오프**(수집→분석 자동 진행).
- **사용자가 실제 마켓 요소를 직접 클릭**한다 — SellerOps가 한 행동을 클릭 시퀀스로 확장하지 않는다.
  감독형 후보-인덱스·fail-closed 원칙 계승(§16.4, collector `review-usage-confirm.ts`·`esm-candidate-signature.ts`).
- 오버레이를 꺼도 **다운로드 감지·인입은 유지**(가이드 없이도 수집 성립).

### 18.2 리뷰 운영 화면 (고객 응대 워크스페이스 §10 확장)
리뷰 운영 화면은 채널별 수집 상태를 **정직한 자율 모드로** 노출한다(진실: `docs/channel-capability-registration-matrix.md`):
- **자동 채널**(AUTOMATIC_OPERATION) — 배경 자동 수집.
- **Action Window 작업**(ACTION_WINDOW) — 사람 체크포인트(사용자가 실제 창에서 export 수행 대기/진행).
- **File Import 작업**(FILE_IMPORT) — 파일 업로드 대기/진행.
- **Integration Pending 채널**(INTEGRATION_PENDING) — "미지원/확인 중"(로드맵 문구 금지, §15).
- **마지막 성공 수집**(last successful acquisition) · **정체/실패 수집**(stale/failed) · **다운스트림 분석
  완료**(수집 후 정규화·분석·리포트 완료 여부).
- 셀러 언어·부분 데이터 고지(§4.6·§13) 계승. 각 상태에 **다음 행동 CTA 1개**.

### 18.3 반응형·프라이버시
- **데스크톱 우선**(로컬 에이전트 데스크톱 런타임 — §16.8). 모바일은 진행·완료 열람만, 실제 조작은 데스크톱 안내.
- 프레임·입력·페이지 내용·다운로드 원문 **비영속·비로깅·백엔드 미경유**(§16.9, G2 §9 계승). raw URL/DOM/
  식별자 미표시. **자동 클립보드·페이지-비밀 추출 없음.** 로컬-전용 프라이버시 고지 상시 노출.

---

### 부록 — 근거 문서
- 범위·Track·frontstage/backstage·가이드 연결·운영 에이전트·자율 모드·pull-first·Session Readiness·Company
  Voice·Action Window 기본: `docs/product-scope-v1.md` (lock v1.7) §1.2·§1.4·§1.5·§1.6·§1.7·§1.8·§5.2·§6.1
- FE 경계(Agent Control Plane projection/command adapter, 동결, minimal Control Center 방향):
  `docs/action-window-runtime/agent-first-ui-light-adr.md`
- 채널 capability 현행표·수집 전략·Action Window 기본·채널 결정: `docs/multi-channel-connector-roadmap.md`
  §4.1·§5·§5.1·§5.2·§11·부록 A
- Action Window 계약·리뷰 운영 화면: `docs/slices/action-window-v1.md`
- 채널 capability × 자율 모드 × 등록 매트릭스: `docs/channel-capability-registration-matrix.md`
- 런타임 경계(BrowserRuntime/CredentialVault/AgentLifecycle·프론트-에이전트 페어링·프로젝션):
  `docs/sellerops_local_agent_runtime_adr.md` (Runtime ADR)
- 검증 기록: `docs/sellerops_phase3c_live_smoke.md`, `docs/sellerops_cafe24_live_verification.md`
- 흡수된 UI 원칙 원문: `docs/sellerops_ui_reference.md` (reference-only)
- 현행(as-is) 상세 근거: 2026-07-07 프론트 정찰·문서 감사·로컬 에이전트 아키텍처 세션 보고
